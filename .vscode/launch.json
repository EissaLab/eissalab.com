{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "Serve site (Docker)",
      "type": "chrome",
      "request": "launch",
      "url": "http://localhost:4000",
      "preLaunchTask": "jekyll: docker compose up",
      "postDebugTask": "jekyll: docker compose down"
    }
  ]
}
